# coding = utf-8
tableData = [['apples', 'oranges', 'cherries', 'banana'],
             ['Alice', 'Bob', 'Carol', 'David'],
             ['dogs', 'cats', 'moose', 'goose']]

def printTable(table_name):
    '''
     colWidth = [0] * len(table_name)

    for k in range(len(table_name)):
        tmp_table = []  #用来存放字典中每个子列表中字符的长度，便于下一步的“max()”
        for v in table_name[k]:
            # lenth = len(j)
            tmp_table.append(len(v))
        colWidth[k]  = max(tmp_table)  # 至此，colWidth列表完成。
        # print(tmp_table)
    '''
#####错了，应该获取列的！！











printTable(tableData)








